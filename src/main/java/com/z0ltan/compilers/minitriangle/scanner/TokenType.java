package com.z0ltan.compilers.minitriangle.scanner;

public enum TokenType {
  BECOMES, // :=
  BEGIN,
  COLON,
  COMMA,
  CONST,
  DO,
  ELSE,
  END,
  EOF,
  FUNCTION,
  IDENTIFIER,
  IF,
  ILLEGAL,
  IN,
  INTEGER_LITERAL,
  IS, // ~
  LEFT_PAREN,
  LET,
  OPERATOR,
  RETURN,
  RIGHT_PAREN,
  SEMICOLON,
  THEN,
  VAR,
  WHILE,
}
