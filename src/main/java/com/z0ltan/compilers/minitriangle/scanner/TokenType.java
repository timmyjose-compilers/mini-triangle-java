package com.z0ltan.compilers.minitriangle.scanner;

public enum TokenType {
  BECOMES, // :=
  BEGIN,
  CHARACTER_LITERAL,
  COLON,
  COMMA,
  CONST,
  DO,
  ELSE,
  END,
  EOF,
  FUNCTION,
  IDENTIFIER,
  IF,
  ILLEGAL,
  IN,
  INTEGER_LITERAL,
  IS, // ~
  LEFT_PAREN,
  LET,
  OPERATOR,
  PROC,
  RETURN,
  RIGHT_PAREN,
  SEMICOLON,
  STRING,
  THEN,
  VAR,
  WHILE,
}
